
// appeler la fonction (une seule fois) au bout d'un certain temps
window.setTimeout( function, temps );

// appeler la fonction tous les X temps
window.setInterval( function, temps ) 

-> reference sur l'objet "interval"



function rappel() {
   // des trucs dedans
   window.setTimeout(rappel, 500);
}
window.setTimeout(rappel, 500);


